__pycache__/
*.pyc
.pytest_cache/
.venv/
.DS_Store
.idea/

# 真實憑證與真實執行設定——絕對不進 git,只留範本(*.example)。
.env
live_execution_config.yaml

# 說明用的工程圖(architecture 討論過程中畫的 SVG)——不上傳,只留在本機。
docs/*.svg

# 外部評估/規劃文件——內部參考用,不上傳。
docs/ROADMAP.md

# 進度追蹤(上一步做了什麼、下一步該做什麼)——內部參考用,不上傳。
TODO.md
